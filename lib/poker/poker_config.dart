import 'dart:ui';

class PokerConfig {
  // idle
  static const int idleCardNum = 2; // idle时上屏的卡片数
  static const double backCardScale = .8;
  static const Offset backCardOffset = Offset(0, .1); // 露出百分比
}
